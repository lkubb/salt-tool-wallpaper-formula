# vim: ft=yaml
#
# Set default values.
---
values:
  lookup:
    paths:
      datadir: '.wallpapers'
      xdg_dirname: 'wallpapers'
    rootgroup: root
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
